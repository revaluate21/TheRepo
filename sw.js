const STATIC='lisbon-quest-static-v7';
const RUNTIME='lisbon-quest-runtime-v7';
const CORE=['./','./index.html','./style.css','./data.js','./app.js','./app-core.js','./app-ui.js','./manifest.webmanifest','./icon.svg'];
const THIRD=['https://unpkg.com/leaflet@1.9.4/dist/leaflet.css','https://unpkg.com/leaflet@1.9.4/dist/leaflet.js'];
self.addEventListener('install',event=>event.waitUntil((async()=>{const c=await caches.open(STATIC);await c.addAll(CORE);for(const u of THIRD){try{const r=await fetch(u,{mode:'cors'});if(r.ok)await c.put(u,r)}catch(e){}}await self.skipWaiting()})()));
self.addEventListener('activate',event=>event.waitUntil((async()=>{const ks=await caches.keys();await Promise.all(ks.filter(k=>![STATIC,RUNTIME].includes(k)).map(k=>caches.delete(k)));await self.clients.claim()})()));
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;const req=event.request,url=new URL(req.url);if(req.mode==='navigate'){event.respondWith((async()=>{try{const fresh=await fetch(req);const c=await caches.open(STATIC);c.put('./index.html',fresh.clone());return fresh}catch(e){return(await caches.match(req))||(await caches.match('./index.html'))}})());return;}const external=url.origin!==self.location.origin;const cacheableExternal=external&&(url.hostname.includes('unpkg.com')||url.hostname.includes('wikimedia.org')||url.hostname.includes('cartocdn.com'));if(cacheableExternal){event.respondWith((async()=>{const c=await caches.open(RUNTIME);const hit=await c.match(req);const net=fetch(req).then(r=>{if(r.ok||r.type==='opaque')c.put(req,r.clone());return r}).catch(()=>null);return hit||(await net)||Response.error()})());return;}if(!external){event.respondWith((async()=>{const c=await caches.open(STATIC);const hit=await c.match(req);if(hit){fetch(req).then(r=>{if(r.ok)c.put(req,r.clone())}).catch(()=>{});return hit}try{const r=await fetch(req);if(r.ok)c.put(req,r.clone());return r}catch(e){return Response.error()}})());}});
